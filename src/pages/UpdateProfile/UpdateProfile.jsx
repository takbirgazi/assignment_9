import { Helmet } from "react-helmet-async";

const UpdateProfile = () => {
    return (
        <div>
            <Helmet>
                <title>Update Profile - Five Star</title>
            </Helmet>
            <h2>This is Update Profile</h2>
        </div>
    );
};

export default UpdateProfile;